name: Traditional CI/CD Pipeline (OLD WAY)

# This shows the TRADITIONAL approach - runs ALL tests every time
# Problem: Slow, expensive, wasteful

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  traditional-tests:
    name: "😴 Run ALL Tests (Traditional Way)"
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🐘 Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, xml, ctype, json, bcmath
          coverage: none
      
      - name: 📦 Install Composer dependencies
        run: composer install --prefer-dist --no-progress --no-suggest
      
      - name: 📋 Copy .env file
        run: cp .env.example .env
      
      - name: 🔑 Generate application key
        run: php artisan key:generate
      
      - name: ⏱️ Start timer
        run: echo "START_TIME=$(date +%s)" >> $GITHUB_ENV
      
      - name: 🧪 Run ALL tests (Traditional - NO AI)
        run: |
          echo \"━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\"
          echo \"  TRADITIONAL PIPELINE - Running ALL Tests\"
          echo \"━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\"
          echo \"\"
          echo \"⚠️  WARNING: This runs EVERY test, every time!\"
          echo \"⚠️  Even if you only changed 1 file...\"
          echo \"⚠️  No intelligence, no optimization\"
          echo \"\"
          
          # Run tests 3 times to simulate thorough CI/CD checks
          echo \"📊 Pass 1: Unit Tests\"
          vendor/bin/phpunit --testsuite=Unit
          
          echo \"\"
          echo \"📊 Pass 2: Integration Validation\"
          vendor/bin/phpunit --testsuite=Unit
          
          echo \"\"
          echo \"📊 Pass 3: Regression Check\"
          vendor/bin/phpunit --testsuite=Unit
          
          echo ""
          echo "✅ All tests passed, but at what cost?"
      
      - name: ⏱️ Calculate time taken
        if: always()
        run: |
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          MINUTES=$((DURATION / 60))
          SECONDS=$((DURATION % 60))
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo \"  ⏰ Time Taken: ${MINUTES}m ${SECONDS}s\"
          echo \"  📊 Tests Run: ALL tests × 3 passes\"
          echo \"  💸 Estimated Cost: \$0.15\"
          echo \"  😤 Developer Experience: Poor\"
          echo \"  🎯 AI Alternative: Would be 5-10x faster!\"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
